# 项目运行说明

## 一、前置条件

### 1. 必需软件

| 软件 | 版本 | 下载地址 |
|------|------|----------|
| JDK | 21+ | [Oracle JDK](https://www.oracle.com/java/technologies/downloads/) |
| Maven | 3.8+ | [Maven](https://maven.apache.org/download.cgi) |
| Docker Desktop | 最新版 | [Docker Desktop](https://www.docker.com/products/docker-desktop/) |
| Git | 最新版 | [Git](https://git-scm.com/downloads) |

### 2. 验证安装

```bash
# 检查 Java 版本
java -version

# 检查 Maven 版本
mvn -version

# 检查 Docker 版本
docker --version
docker-compose --version
```

## 二、项目结构

```
Distributed-System-work/
├── pom.xml                           # 父POM
├── docker-compose.yml                # Docker 编排文件
├── user-service/                     # 用户服务（8081/8082）
├── product-service/                  # 商品服务（8083）
├── inventory-service/                # 库存服务（8084）
├── order-service/                    # 订单服务（8085）
├── api-gateway/                      # API 网关（8080）
├── common/                           # 公共模块
└── nginx/                            # Nginx 配置
```

## 三、运行方式

### 方式一：本地开发模式（推荐）

#### 步骤 1：启动中间件

**方法 A：使用 Docker Compose 启动所有中间件**

```bash
# 在项目根目录执行
docker-compose up -d mysql redis rabbitmq
```

**方法 B：单独启动中间件**

```bash
# 启动 MySQL
docker run -d --name seckill-mysql \
  -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=verysecret \
  -e MYSQL_DATABASE=seckill \
  -e MYSQL_USER=seckill \
  -e MYSQL_PASSWORD=secret \
  mysql:8.0

# 启动 Redis
docker run -d --name seckill-redis \
  -p 6379:6379 \
  redis:7.0-alpine

# 启动 RabbitMQ
docker run -d --name seckill-rabbitmq \
  -p 5672:5672 \
  -p 15672:15672 \
  -e RABBITMQ_DEFAULT_USER=guest \
  -e RABBITMQ_DEFAULT_PASS=guest \
  rabbitmq:3.12-management
```

#### 步骤 2：启动用户服务（多实例）

**打开第一个终端，启动用户服务实例 1（端口 8081）：**

```bash
cd user-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8081"
```

**打开第二个终端，启动用户服务实例 2（端口 8082）：**

```bash
cd user-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8082"
```

#### 步骤 3：启动其他服务

**商品服务（端口 8083）：**

```bash
cd product-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8083"
```

**库存服务（端口 8084）：**

```bash
cd inventory-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8084"
```

**订单服务（端口 8085）：**

```bash
cd order-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8085"
```

**API 网关（端口 8080）：**

```bash
cd api-gateway
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8080"
```

#### 步骤 4：验证服务启动

访问以下 URL 验证服务是否正常：

- 用户服务 1: http://localhost:8081/api/users/health
- 用户服务 2: http://localhost:8082/api/users/health
- 商品服务：http://localhost:8083/api/products/health
- 库存服务：http://localhost:8084/api/inventory/health
- 订单服务：http://localhost:8085/api/orders/health
- API 网关：http://localhost:8080/actuator/health

### 方式二：Docker Compose 一键启动

#### 步骤 1：构建所有服务

```bash
# 在项目根目录执行
mvn clean package -DskipTests
```

#### 步骤 2：启动所有服务

```bash
# 在项目根目录执行
docker-compose up -d
```

#### 步骤 3：查看服务状态

```bash
# 查看所有服务状态
docker-compose ps

# 查看服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f user-service-1
```

#### 步骤 4：停止服务

```bash
# 停止所有服务
docker-compose down

# 停止并删除数据卷
docker-compose down -v
```

## 四、测试用户注册登录

### 1. 测试用户注册

**使用 curl 命令：**

```bash
curl -X POST http://localhost:8081/api/users/register \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"testuser\",\"password\":\"123456\",\"email\":\"test@example.com\"}"
```

**使用 Postman：**
- Method: POST
- URL: http://localhost:8081/api/users/register
- Headers: Content-Type: application/json
- Body (raw JSON):
```json
{
  "username": "testuser",
  "password": "123456",
  "email": "test@example.com"
}
```

**预期响应：**
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    "createTime": "2024-01-01T12:00:00",
    "updateTime": "2024-01-01T12:00:00"
  }
}
```

### 2. 测试用户登录

**使用 curl 命令：**

```bash
curl -X POST http://localhost:8081/api/users/login \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"testuser\",\"password\":\"123456\"}"
```

**使用 Postman：**
- Method: POST
- URL: http://localhost:8081/api/users/login
- Headers: Content-Type: application/json
- Body (raw JSON):
```json
{
  "username": "testuser",
  "password": "123456"
}
```

**预期响应：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "username": "testuser",
    "token": "eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiIxIiwidXNlcm5hbWUiOiJ0ZXN0dXNlciIsImlhdCI6MTcwNjcwMDAwMCwiZXhwIjoxNzA2Nzg2NDAwfQ.xxx"
  }
}
```

### 3. 查询用户信息

**使用 curl 命令：**

```bash
curl http://localhost:8081/api/users/1
```

**预期响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com"
  }
}
```

## 五、常见问题

### 1. 端口冲突

**问题：** 启动时提示端口已被占用

**解决方案：**
```bash
# Windows 查看端口占用
netstat -ano | findstr :8081

# 杀死占用端口的进程
taskkill /F /PID <进程 ID>
```

### 2. MySQL 连接失败

**问题：** 启动时提示无法连接 MySQL

**解决方案：**
```bash
# 检查 MySQL 是否启动
docker ps | grep mysql

# 查看 MySQL 日志
docker logs seckill-mysql

# 重启 MySQL
docker restart seckill-mysql
```

### 3. Redis 连接失败

**问题：** 启动时提示无法连接 Redis

**解决方案：**
```bash
# 检查 Redis 是否启动
docker ps | grep redis

# 重启 Redis
docker restart seckill-redis
```

### 4. Maven 依赖下载失败

**问题：** Maven 下载依赖超时或失败

**解决方案：**
```bash
# 使用国内镜像（阿里云）
# 在 settings.xml 中添加：
<mirror>
  <id>aliyunmaven</id>
  <mirrorOf>*</mirrorOf>
  <name>阿里云公共仓库</name>
  <url>https://maven.aliyun.com/repository/public</url>
</mirror>
```

### 5. Docker Compose 启动失败

**问题：** docker-compose up 报错

**解决方案：**
```bash
# 清理 Docker 资源
docker-compose down -v

# 重新构建
docker-compose up -d --build

# 查看详细日志
docker-compose logs
```

## 六、服务端口一览

| 服务 | 端口 | 说明 |
|------|------|------|
| Nginx | 80 | 负载均衡 + 动静分离 |
| API Gateway | 8080 | API 网关 |
| User Service 1 | 8081 | 用户服务实例 1 |
| User Service 2 | 8082 | 用户服务实例 2 |
| Product Service | 8083 | 商品服务 |
| Inventory Service | 8084 | 库存服务 |
| Order Service | 8085 | 订单服务 |
| MySQL | 3306 | 数据库 |
| Redis | 6379 | 缓存 |
| RabbitMQ | 5672 | 消息队列 |

## 七、API 接口文档

### 用户服务 API

#### 1. 用户注册
```
POST /api/users/register
Content-Type: application/json

{
  "username": "testuser",
  "password": "123456",
  "email": "test@example.com"
}
```

#### 2. 用户登录
```
POST /api/users/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "123456"
}

Response:
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "username": "testuser",
    "token": "xxx"
  }
}
```

#### 3. 查询用户
```
GET /api/users/{id}
```

#### 4. 健康检查
```
GET /api/users/health
```

## 八、开发建议

### 1. IDE 配置

**推荐使用 IntelliJ IDEA：**
1. 打开项目：File → Open → 选择项目根目录
2. 等待 Maven 依赖下载完成
3. 配置 Lombok 插件：File → Settings → Plugins → 安装 Lombok

### 2. 数据库连接

**本地开发时，确保 application.properties 中的数据库连接正确：**
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/seckill?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=verysecret
```

### 3. 日志查看

**启动日志：**
```bash
# 查看用户服务日志
docker logs seckill-user-service-1

# 实时查看日志
docker logs -f seckill-user-service-1
```

### 4. 热部署

**启用热部署（开发模式）：**
```bash
# 在 IDE 中运行，修改代码后会自动重启
mvn spring-boot:run
```

## 九、下一步

1. ✅ 用户注册登录功能已完成
2. ⏳ 实现商品 CRUD 功能
3. ⏳ 实现秒杀核心逻辑
4. ⏳ 实现订单创建功能
5. ⏳ JMeter 压力测试

祝你开发顺利！🎉

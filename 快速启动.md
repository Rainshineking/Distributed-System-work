# 🚀 快速启动指南

## 一、前置检查

### 1. 检查软件安装

```bash
# 检查 Java
java -version

# 检查 Maven
mvn -version

# 检查 Docker
docker --version
docker-compose --version
```

### 2. 启动 Docker Desktop

确保 Docker Desktop 正在运行。

## 二、快速启动（3 步完成）

### 步骤 1：启动中间件

**在项目根目录打开 PowerShell，执行：**

```powershell
docker-compose up -d mysql redis rabbitmq
```

等待 30 秒，让中间件完全启动。

### 步骤 2：启动用户服务

**打开终端 1（用户服务实例 1 - 8081）：**

```powershell
cd user-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8081"
```

等待看到以下日志表示启动成功：
```
Started UserSystemApplication in X.XXX seconds
```

**打开终端 2（用户服务实例 2 - 8082）：**

```powershell
cd user-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8082"
```

### 步骤 3：测试

**打开新的终端窗口：**

```powershell
# 测试健康检查
curl http://localhost:8081/api/users/health

# 测试用户注册
curl -X POST http://localhost:8081/api/users/register `
  -H "Content-Type: application/json" `
  -d "{\"username\":\"testuser\",\"password\":\"123456\",\"email\":\"test@example.com\"}"

# 测试用户登录
curl -X POST http://localhost:8081/api/users/login `
  -H "Content-Type: application/json" `
  -d "{\"username\":\"testuser\",\"password\":\"123456\"}"
```

## 三、预期输出

### 健康检查
```json
{
  "code": 200,
  "message": "User Service is running",
  "data": null
}
```

### 用户注册
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com"
  }
}
```

### 用户登录
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "username": "testuser",
    "token": "eyJhbGciOiJIUzUxMiJ9.xxx"
  }
}
```

## 四、使用脚本启动（更简单）

### 启动服务

```powershell
# 在项目根目录执行
.\start.ps1
```

脚本会自动：
1. 检查 Docker 状态
2. 启动中间件（MySQL, Redis, RabbitMQ）
3. 提示你启动各个服务

### 停止服务

```powershell
# 在项目根目录执行
.\stop.ps1
```

## 五、服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| 用户服务 1 | 8081 | 用户服务实例 1 |
| 用户服务 2 | 8082 | 用户服务实例 2 |
| 商品服务 | 8083 | 商品服务（待启动） |
| 库存服务 | 8084 | 库存服务（待启动） |
| 订单服务 | 8085 | 订单服务（待启动） |
| API 网关 | 8080 | API 网关（待启动） |
| MySQL | 3306 | 数据库 |
| Redis | 6379 | 缓存 |
| RabbitMQ | 5672 | 消息队列 |

## 六、常见问题

### 问题 1：端口被占用

**错误信息：**
```
Port 8081 is already in use
```

**解决方法：**
```powershell
# 查看占用端口的进程
netstat -ano | findstr :8081

# 杀死进程
taskkill /F /PID <进程 ID>
```

### 问题 2：MySQL 连接失败

**错误信息：**
```
com.mysql.cj.jdbc.exceptions.CommunicationsException
```

**解决方法：**
```powershell
# 检查 MySQL 是否启动
docker ps | grep mysql

# 如果没有启动，重新启动
docker-compose up -d mysql
```

### 问题 3：Maven 依赖下载失败

**错误信息：**
```
Could not resolve dependencies
```

**解决方法：**
```powershell
# 使用阿里云镜像
mvn clean install -U
```

## 七、验证数据库

### 连接数据库

```powershell
docker exec -it seckill-mysql mysql -uroot -pverysecret seckill
```

### 查看用户数据

```sql
-- 查看所有用户
SELECT * FROM user;

-- 退出
EXIT;
```

## 八、停止服务

### 方法 1：使用脚本

```powershell
.\stop.ps1
```

### 方法 2：手动停止

```powershell
# 停止所有 Docker 容器
docker-compose down

# 停止并清理数据
docker-compose down -v
```

## 九、下一步

1. ✅ 用户注册登录功能 - **已完成**
2. ⏳ 商品 CRUD 功能
3. ⏳ 秒杀核心逻辑
4. ⏳ 订单创建功能
5. ⏳ JMeter 压力测试

---

**启动成功！开始测试吧！** 🎉

详细文档请查看：
- [项目运行说明.md](项目运行说明.md)
- [快速测试指南.md](快速测试指南.md)
- [项目实现总结.md](项目实现总结.md)

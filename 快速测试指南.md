# 快速测试指南

## 一、启动服务

### 方法 1：使用启动脚本（推荐）

```powershell
# 在项目根目录执行
.\start.ps1
```

然后按照提示打开多个终端启动各个服务。

### 方法 2：手动启动

**终端 1 - 启动 MySQL、Redis、RabbitMQ：**
```bash
docker-compose up -d mysql redis rabbitmq
```

**终端 2 - 启动用户服务实例 1（8081）：**
```bash
cd user-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8081"
```

**终端 3 - 启动用户服务实例 2（8082）：**
```bash
cd user-service
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=8082"
```

## 二、测试 API

### 1. 健康检查

```bash
# 检查用户服务 1
curl http://localhost:8081/api/users/health

# 检查用户服务 2
curl http://localhost:8082/api/users/health

# 预期响应：
# {"code":200,"message":"User Service is running","data":null}
```

### 2. 用户注册

**使用 curl：**
```bash
curl -X POST http://localhost:8081/api/users/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"testuser\",\"password\":\"123456\",\"email\":\"test@example.com\"}"
```

**使用 PowerShell：**
```powershell
$body = @{
    username = "testuser"
    password = "123456"
    email = "test@example.com"
} | ConvertTo-Json

Invoke-RestMethod -Method POST `
  -Uri "http://localhost:8081/api/users/register" `
  -ContentType "application/json" `
  -Body $body
```

**预期响应：**
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    "createTime": "2024-01-01T12:00:00",
    "updateTime": "2024-01-01T12:00:00"
  }
}
```

### 3. 用户登录

**使用 curl：**
```bash
curl -X POST http://localhost:8081/api/users/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"testuser\",\"password\":\"123456\"}"
```

**使用 PowerShell：**
```powershell
$body = @{
    username = "testuser"
    password = "123456"
} | ConvertTo-Json

$response = Invoke-RestMethod -Method POST `
  -Uri "http://localhost:8081/api/users/login" `
  -ContentType "application/json" `
  -Body $body

# 保存 Token
$token = $response.data.token
Write-Host "Token: $token"
```

**预期响应：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "username": "testuser",
    "token": "eyJhbGciOiJIUzUxMiJ9.xxx"
  }
}
```

### 4. 查询用户信息

**使用 curl：**
```bash
# 根据 ID 查询
curl http://localhost:8081/api/users/1

# 根据用户名查询
curl http://localhost:8081/api/users/username/testuser
```

**使用 PowerShell：**
```powershell
# 根据 ID 查询
Invoke-RestMethod -Method GET -Uri "http://localhost:8081/api/users/1"

# 根据用户名查询
Invoke-RestMethod -Method GET -Uri "http://localhost:8081/api/users/username/testuser"
```

**预期响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com"
  }
}
```

## 三、使用 Postman 测试

### 1. 用户注册

**配置：**
- Method: POST
- URL: http://localhost:8081/api/users/register
- Headers: Content-Type: application/json
- Body (raw JSON):
```json
{
  "username": "testuser",
  "password": "123456",
  "email": "test@example.com"
}
```

### 2. 用户登录

**配置：**
- Method: POST
- URL: http://localhost:8081/api/users/login
- Headers: Content-Type: application/json
- Body (raw JSON):
```json
{
  "username": "testuser",
  "password": "123456"
}
```

**保存 Token：**
登录后复制响应中的 `token` 字段值。

### 3. 查询用户（带 Token）

**配置：**
- Method: GET
- URL: http://localhost:8081/api/users/1
- Headers:
  - Content-Type: application/json
  - Authorization: Bearer {你的 token}

## 四、测试场景

### 场景 1：正常注册登录流程

```bash
# 1. 注册新用户
curl -X POST http://localhost:8081/api/users/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"zhangsan\",\"password\":\"123456\",\"email\":\"zhangsan@example.com\"}"

# 2. 登录
curl -X POST http://localhost:8081/api/users/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"zhangsan\",\"password\":\"123456\"}"

# 3. 查询用户信息
curl http://localhost:8081/api/users/1
```

### 场景 2：用户名重复

```bash
# 1. 第一次注册（成功）
curl -X POST http://localhost:8081/api/users/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"lisi\",\"password\":\"123456\",\"email\":\"lisi1@example.com\"}"

# 2. 第二次注册（失败 - 用户名已存在）
curl -X POST http://localhost:8081/api/users/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"lisi\",\"password\":\"123456\",\"email\":\"lisi2@example.com\"}"

# 预期响应：
# {"code":500,"message":"用户名已存在","data":null}
```

### 场景 3：错误密码

```bash
# 使用错误密码登录
curl -X POST http://localhost:8081/api/users/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"zhangsan\",\"password\":\"wrongpassword\"}"

# 预期响应：
# {"code":500,"message":"用户名或密码错误","data":null}
```

### 场景 4：负载均衡测试

```bash
# 多次请求，观察由哪个服务实例处理
for ($i=1; $i -le 5; $i++) {
    Write-Host "请求 $i :"
    curl http://localhost:8081/api/users/health
    Write-Host ""
}
```

## 五、数据库验证

### 1. 连接数据库

```bash
# 使用 MySQL 客户端连接
docker exec -it seckill-mysql mysql -uroot -pverysecret seckill
```

### 2. 查看用户表

```sql
-- 查看所有用户
SELECT * FROM user;

-- 查看特定用户
SELECT * FROM user WHERE username = 'testuser';

-- 查看用户数量
SELECT COUNT(*) FROM user;
```

### 3. 验证数据

```sql
-- 应该能看到注册的用户
-- id | username | password (加密) | email | create_time | update_time
-- 1  | testuser | $2a$10$...      | test@example.com | 2024-01-01 12:00:00 | ...
```

## 六、常见问题

### 1. 注册失败 - 数据库连接错误

**问题：**
```
com.mysql.cj.jdbc.exceptions.CommunicationsException: Communications link failure
```

**解决：**
```bash
# 检查 MySQL 是否启动
docker ps | grep mysql

# 如果没有启动，重新启动
docker-compose up -d mysql
```

### 2. 登录失败 - 用户不存在

**问题：**
```
用户名或密码错误
```

**解决：**
- 确认用户已注册
- 检查用户名拼写
- 检查密码是否正确

### 3. 端口冲突

**问题：**
```
Port 8081 is already in use
```

**解决：**
```bash
# 查看占用端口的进程
netstat -ano | findstr :8081

# 杀死进程
taskkill /F /PID <进程 ID>
```

### 4. Maven 构建失败

**问题：**
```
Could not resolve dependencies
```

**解决：**
```bash
# 清理并重新构建
mvn clean install -U

# 使用国内镜像
# 在 settings.xml 中配置阿里云镜像
```

## 七、性能测试

### 使用 JMeter 进行压力测试

**步骤：**
1. 打开 JMeter
2. 创建测试计划
3. 添加线程组
4. 添加 HTTP Request
5. 运行测试

**测试配置示例：**
- 线程数：100
- Ramp-Up 时间：10 秒
- 循环次数：10

**测试接口：**
```
GET http://localhost:8081/api/users/health
```

## 八、监控与日志

### 1. 查看服务日志

```bash
# 查看用户服务日志
docker logs seckill-user-service-1

# 实时查看日志
docker logs -f seckill-user-service-1

# 查看最近 100 行日志
docker logs --tail 100 seckill-user-service-1
```

### 2. 查看数据库日志

```bash
docker logs seckill-mysql
```

### 3. 查看 Redis 日志

```bash
docker logs seckill-redis
```

## 九、停止服务

```bash
# 停止所有服务
docker-compose down

# 停止并清理数据
docker-compose down -v

# 或者使用停止脚本
.\stop.ps1
```

---

测试愉快！如有问题请查看 [项目运行说明.md](项目运行说明.md) 🎉
